@../AGENTS.md

Reglas: `../.cursor/rules/frontend-agent.mdc` (symlink: `.cursor/rules/agent.mdc`). Skills: **solo** `../.cursor/skills/` en la raíz del monorepo (referencia `.cursor/skills/...` con workspace en `stellar-build/`). Stellar: `.cursor/skills/stellar/`.
