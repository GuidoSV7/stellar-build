---
description: Punto de entrada del agente para contratos Stellar (Soroban) en stellar-build.
alwaysApply: true
globs:
  - contract/**
---

# AGENTS.md — Contract (BolivianPets / stellar-build)

Punto de entrada del agente en `contract/`. Symlink: `contract/.cursor/rules/agent.mdc`.

## Git

- **Nunca** poner `Co-authored-by: Cursor` (ni variantes `cursoragent` / `*@cursor*`) en commits.

## Stack

- Contratos **Soroban** (Rust / `soroban-sdk`) en `contract/`
- Skills Stellar: `.cursor/skills/stellar/` (ex-`.claude/skills`)

## Autoinvocación de Skills — OBLIGATORIO

| Tarea | Skill a leer |
|---|---|
| Setup, anatomía, build/deploy Soroban | `.cursor/skills/stellar/smart-contracts/SKILL.md` |
| Assets / trustlines / SAC | `.cursor/skills/stellar/assets/SKILL.md` |
| Standards (SEPs/CAPs) | `.cursor/skills/stellar/standards/SKILL.md` |
| Pagos agentic / x402 | `.cursor/skills/stellar/agentic-payments/SKILL.md` |
| ZK / privacy | `.cursor/skills/stellar/zk-proofs/SKILL.md` |
| Deploy mainnet | `.cursor/skills/stellar/deploy-stellar-mainnet/SKILL.md` |

## Enrutamiento

- Subagent: `contract/.cursor/agents/contract.md`
- Router general: `.cursor/skills/stellar/SKILL_ROUTER.md`
- Gate: toolchain Soroban del package `contract/` (cuando exista)
